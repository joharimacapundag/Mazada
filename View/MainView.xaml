 <Window x:Class="Mazada.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mazada"
        xmlns:viewModels="clr-namespace:Mazada.ViewModel"
        xmlns:views="clr-namespace:Mazada.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
         >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ProgressBar Grid.Row="1" Width="Auto" Height="25" Margin="5" Value="{Binding ProgressBarTask}" BorderBrush="Black" Maximum="4"/>
        </Grid>

        <ContentControl Grid.ColumnSpan="3" Grid.Row="0" Content="{Binding CurrentViewModel}" />
        
        <StackPanel Grid.ColumnSpan="3" Grid.Row="1" Orientation="Horizontal" Margin="5,0,5,0">
            <Button Command="{Binding LoginCommand}" Content="Login" Margin="5"  Width="100" Height="24"/>
            <Button Command="{Binding HomeCommand}" Content="Home" Margin="5" Width="100" Height="24"/>
            <Button Command="{Binding ProductsCommand}" Content="Products" Margin="5" Width="100" Height="24"/>
            <Button Command="{Binding ProductCommand}" Content="Product Detail" Margin="5" Width="100" Height="24"/>
            <Button Command="{Binding BackCommand}" Content="Go Back" Margin="5" Width="100" Height="24"/>
        </StackPanel>
        
    </Grid>
</Window>
