<UserControl x:Class="Mazada.View.ProductDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mazada.View"
             xmlns:subviews="clr-namespace:Mazada.View.Subview"
             xmlns:services="clr-namespace:Mazada.Services"
             Background="#F5F5F5"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="800">
    <UserControl.Resources>
        <services:UsdToPhpConverter x:Key="UsdToPhp" ExchangeRate="56.5"/>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height=".15*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".2*"/>
                <ColumnDefinition MinWidth="320"/>
                <ColumnDefinition Width=".2*"/>
            </Grid.ColumnDefinitions>
            <Rectangle Grid.Column="0" Grid.ColumnSpan="3" Fill="#666666"/>
            <subviews:HeaderView Grid.Row="0" Grid.Column="1"/>
            <Grid Grid.Row="1" Grid.Column="1" Background="White">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition MinWidth="320" />
                </Grid.ColumnDefinitions>
                <Rectangle Grid.Column="0" Fill="DarkGray" />
                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Hidden">
                    <StackPanel >
                        <Rectangle Fill="DarkGray" Height="250" Margin="8,10,8,0"/>
                        <Label Content="{Binding Product.Name}" Margin="5,10,5,0" FontSize="16"/>
                        <Rectangle Fill="DarkGray" Height="50" Margin="8,10,8,0"/>
                        <StackPanel Margin="5,10,5,0" Orientation="Horizontal">
                            <Label Content="Price:" FontSize="16"/>
                            <Label Content="{Binding Product.Price, Converter={StaticResource UsdToPhp}}" FontSize="16"/>
                        </StackPanel>
                        <StackPanel Margin="5,10,5,0" Orientation="Horizontal">
                            <Label Content="Quantity" FontSize="16"/>
                            <StackPanel Orientation="Horizontal" >
                                <Button Command="{Binding DecreaseCommand}" Content="−" Width="25" Height="25" FontSize="16"/>
                                <TextBlock Text="{Binding Quantity}" TextAlignment="Center" Width="25" Height="25" FontSize="16"/>
                                <Button Command="{Binding IncreaseCommand}" Content="+" Width="25" Height="25" FontSize="16"/>
                            </StackPanel>
                            
                        </StackPanel>
                        <Grid Margin="5,10,5,0"  >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Button Command="{Binding BuyCommand}" Grid.Column="0" Content="Buy Now" Margin="0,0,4,0" FontSize="16"/>
                            <Button Command="{Binding AddToCartCommand}" Grid.Column="1" Content="Add to Cart" Background="DarkGray" Margin="4,0,0,0" FontSize="16"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
