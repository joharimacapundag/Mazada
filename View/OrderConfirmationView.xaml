<UserControl x:Class="Mazada.View.OrderConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mazada.View"
             xmlns:subviews="clr-namespace:Mazada.View.Subview"
             mc:Ignorable="d" 
             Background="#F5F5F5"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="OrderItemTemplate">
            <StackPanel Orientation="Horizontal">
                <StackPanel>
                    <Label Content="Test"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".2*"/>
                <ColumnDefinition MinWidth="320"/>
                <ColumnDefinition Width=".2*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".2*"/>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width=".2*"/>
                        <ColumnDefinition Width=".2*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </Grid>
            <ListView Grid.Row="1" Grid.Column="1" Background="White" ItemTemplate="{StaticResource OrderItemTemplate}">
                
            </ListView>
        </Grid>
    </Grid>
</UserControl>
