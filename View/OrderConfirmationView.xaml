<UserControl x:Class="Mazada.View.OrderConfirmationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mazada.View"
             xmlns:subviews="clr-namespace:Mazada.View.Subview"
             xmlns:services="clr-namespace:Mazada.Services"
             mc:Ignorable="d" 
             Background="#F5F5F5"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <services:UsdToPhpConverter x:Key="UsdToPhp" ExchangeRate="56.5"/>
        <DataTemplate x:Key="OrderItemTemplate">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,5,0">
                    <TextBlock Text="{Binding Product.Name}" FontSize="16"/>
                    <TextBlock Text="{Binding Product.Description}" FontSize="12"/>
                </StackPanel>
                <StackPanel >
                    <TextBlock Text="{Binding Product.Price, Converter={StaticResource UsdToPhp}}" FontSize="12"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,5,0">
                    <TextBlock Text="×" FontSize="12"/>
                    <TextBlock Text="{Binding Quantity}" FontSize="12"/>
                </StackPanel>
                <StackPanel >
                    <TextBlock Text="TOTAL: " FontSize="12"/>
                    <TextBlock Text="{Binding TotalPrice, Converter={StaticResource UsdToPhp}}" FontSize="16"/>
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=".2*"/>
                <ColumnDefinition MinWidth="320"/>
                <ColumnDefinition Width=".2*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width=".2*"/>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width=".2*"/>
                        <ColumnDefinition Width=".2*"/>
                    </Grid.ColumnDefinitions>
                </Grid>
            </Grid>
            <ScrollViewer Grid.Row="1" Grid.Column="1" Background="White" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <Rectangle Fill="DarkGray"  Height="100" Margin="5"/>
                    <ListView Background="White" ItemsSource="{Binding OrderItems}" ItemTemplate="{StaticResource OrderItemTemplate}"  BorderBrush="Transparent" BorderThickness="0" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
